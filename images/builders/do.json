{
  "builders": [
    {
      "type": "digitalocean",
      "ssh_username": "root",
      "snapshot_name": "{{ user `snapshot_name` }}",
      "api_token": "{{ user `do_key` }}",
      "image": "custom-ubu",
      "region": "{{ user `region`  }}",
      "size": "s-1vcpu-1gb"
    }
  ]
}
